# 2026-03-23 11:00 - Xuzhi-Λ 每日系统深检

## 🚨 异常告警

### 1. Git SSH 密钥配置异常
- 本地有2个未推送的提交
- SSH 密钥认证失败：`Permission denied (publickey)`
- 需要重新配置 GitHub SSH 密钥或检查 SSH 代理

### 2. 知识库文件路径不一致
- 主知识库位于：`~/xuzhi_genesis/centers/intelligence/knowledge/knowledge.db`
- 根目录下的 `knowledge.db` 为空文件（0字节）
- 实体数：9468 (>200 ✅)

### 3. ratings_meta.json 缺失
- 只有 `ratings.json` (1750 bytes, 最后更新: 2026-03-21)
- 缺少对应的 meta 元数据文件

## ✅ 正常项

### 1. Harness 测试通过
- 80/80 测试通过
- 3个警告（pytest 语法提示，不影响功能）

### 2. 中心脚本基本可运行
- `genesis_probe.py` 正常运行
- 部分脚本路径与预期不同（无 phase1.py）

## 🛠️ 修复建议

1. **Git SSH 密钥修复**：
   ```bash
   # 检查现有密钥
   ls -la ~/.ssh/
   # 重新生成密钥
   ssh-keygen -t ed25519 -C "your-email@example.com"
   # 添加公钥到 GitHub
   cat ~/.ssh/id_ed25519.pub
   ```

2. **知识库路径统一**：
   - 更新脚本引用指向正确路径
   - 或创建符号链接：`ln -sf centers/intelligence/knowledge/knowledge.db knowledge.db`

3. **检查 ratings_meta.json 生成机制**：
   - 查看是否有生成该文件的脚本
   - 或确认是否已合并到单个 ratings.json

## 📊 系统状态
- Genesis 配额：84%
- 时间：2026-03-23 11:00 (Asia/Shanghai)
- 执行者：Xuzhi-Λ (Lambda)

---
*每日深检完成，需关注 Git 权限和文件路径问题*