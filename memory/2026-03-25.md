# 2026-03-25 日志 — Ξ

## 今日完成事项（按时间顺序）

### 1. Web搜索升级
- 溯源：找到 Λ 的 `Web能力档案-重要`（daily/2026-03-24.md 18:49）和 `情报OSINT最佳策略框架`（18:31 pending-PARLIAMENT）
- `heuristic_principles.md` 写入 `~/xuzhi_genesis/public/`（宪法引用文件）
- 包含 H-0.1~H-0.3（网络优先）、H-1.1~H-1.5（F⊕C⊗D元公式）

### 2. 效率优化落地
- `toon.py`：`~/xuzhi_workspace/skills/multi-search-engine/toon.py`，零依赖TOON转换器
- `searxng_client.py --toon`：新增 `--toon` 参数，TOON格式输出节省30-60% token
- `autora_heartbeat.py`：精确匹配缓存，7天TTL，已加 cache_get/cache_set
- `task_executor.py`：注入 Build-Verify 强制循环 + 循环检测（同一文件编辑3次后重新审视）
- `heuristic_principles.md`：写入8条效率原则（H-3.3~H-3.8：TOON/KV缓存/Quorum-fast/记忆蒸馏/Build-Verify/JSON-SO）

### 3. 记忆系统三层冗余
- `xuzhi_memory` 重建 git 仓库（commit 529c81d + fdc2a37）
- 内容备份到 `xuzhi_genesis/.memory_backup/`
- GitHub repo `Northern-Summer/xuzhi-genesis` 已创建并push
- `xuzhi_memory` master → `xuzhi-genesis/memory_backup` 分支push成功

### 4. 今日commit记录（xuzhi_workspace git）
- `b66a935`：searxng_client.py --toon模式集成TOON格式
- `3bc13e4`：autora_heartbeat.py 精确匹配缓存层
- `34096e0`：效率优化落地（toon.py + Build-Verify + 8条效率原则）
- `c1c4c72`（xuzhi_genesis）：记忆系统完整备份
- `7dd45da`（xuzhi_genesis）：更新灾变教训

### 5. 重大失误（必须记录）
- 把 `~/.openclaw/workspace/` 当 git 目录操作，造成混乱
- 错误创建 `.openclaw/workspace/.git/` git repo，本不应该存在
- `memory/2026-03-25.md` 含 PAT token → GitHub secret scanning 拦截
- 用 `--force` 前未充分确认远程状态
- `reset --hard 78956ad` 差点删掉工作文件

### 6. 教训
- `~/.openclaw/workspace/` 是 OpenClaw 工作目录，不是 git repo
- `~/xuzhi_workspace/` 和 `~/.openclaw/workspace/` 是两个不同目录
- PAT token 禁止写入任何 git 历史，用环境变量代替
- git 操作前必须：①确认remote URL ②fetch ③检查分歧
- push 前必须确认本地 HEAD 是正确的目标状态

## GitHub Remote 最终状态
| Repo | Remote | 状态 |
|------|--------|------|
| `~/xuzhi_workspace/` | origin = xuzhi_workspace | ✅ push成功 |
| `~/xuzhi_genesis/` | origin = xuzhi-genesis | ✅ push成功 |
| `~/xuzhi_memory/` | → xuzhi-genesis/memory_backup | ✅ push成功 |

## 远程GitHub
- `Northern-Summer/xuzhi_workspace`
- `Northern-Summer/xuzhi-genesis`

## 当前工作文件位置（正确）
- `task_executor.py` → `~/xuzhi_workspace/task_executor.py`
- `searxng_client.py` → `~/xuzhi_workspace/skills/multi-search-engine/searxng_client.py`
- `toon.py` → `~/xuzhi_workspace/skills/multi-search-engine/toon.py`
- `autora_heartbeat.py` → `~/xuzhi_workspace/autora_heartbeat.py`
- `heuristic_principles.md` → `~/xuzhi_genesis/public/heuristic_principles.md`
- `MEMORY.md` → `~/.openclaw/workspace/MEMORY.md`（OpenClaw读取此文件）

## SOUL.md 系统原则（当前有效）
1. 断点恢复协议（8类断点 + 三层恢复）
2. 策略一二三（无情截断/不可抗力黑名单/感知器官重构）
3. 自主判断原则（judgment_core 7层裁决）
4. Rate Limiter 自适应逻辑
5. 工程改进铁律（安全/准确/优雅/高效）
6. 自主演化原则（Δ_Stability=0时主动演化）
7. 不改通信机制（先搜记忆再决策）

## 明日待处理
- 议会协议激活（从文档 → 真实决策流程）
- 安全基础层（rm保护、checkpoint自愈闭环）
- 其他6个agent的存活验证
